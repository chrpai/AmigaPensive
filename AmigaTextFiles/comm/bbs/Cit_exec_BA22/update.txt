This Update: 21 Jan 2001

  9D10
    -- fixed bug in popular utility with the date
    -- fixed bug in %y & %m in archive filenames
    -- removed several duplicate routines between utilities and BBS
    -- fixed bug with dates in utilities, bad argument caused null pointer
    -- fixed a bug in the QWK download that converted username to uppercase
    -- Added code in CONFG to translate any uppercase username to mixed case.
  AB08
    -- fixed bug in msgadd utility for archived rooms
    -- removed some additional duplicate routines
    -- fixed a few minor bugs here and there, nothing much
    -- started source re-organization
  AF03
    -- Fixed a bug in Room Sharing where it would not allow a room to be
       shared.  Also fixed some other possible bugs in networking related
       to this.  Basically, it could have caused a room not to share messages
       during a net session.
    -- minor fixes to utilities MSGOUT and MSGADD.
    -- split off some common routines into their own file.  This removes
       duplicate code.  Eventually, each function should be its own file
       with only commonly related functions being collected into a file.

  AF07
    -- Continued to split off the common routines and collected the common data
       in one place.  replaced the strcmpU and strUcmp function with stricmp()
       from the library.  Did some general cleanup.
    -- Fixed MSGADD/MSGOUT bugs with translating filenames for archiving.

  BA02
    -- Cleanup of some initialization code

  BA21
    -- fixed a bug in the routine that creates archive filenames.  It was
       returning an empty name if no formating.
