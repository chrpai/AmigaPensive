;$HBBS

;  Name=<Conf Access Name (set to same name as access levels where possible>
;  Default=YES|NO      (Optional, Defaults to NO)
;  Override_XX=YES     (Optional, Only set to YES if specified)
;  SeeDefault=YES|NO   (Optional, Defaults to NO)
;  SeeOverride_XX=YES  (Optional, Only set to YES if specified)

Name=Group Member
Default=YES
Override_1=YES
Override_5=YES
SeeDefault=YES
SeeOverride_1=YES
SeeOverride_5=YES

; Access to all confs except 1 & 5, also can't even see conference 1 & 5
