.key node,file1,workdir,playpendir,file
.bra {
.ket }
ram:
del ram:file_id.diz >nil:
if exists {playpendir}{file}.lha
  lha x {playpendir}{file} FILE_ID.DIZ ram: >nil:
endif
if exists {playpendir}{file}.lzh
  lha x {playpendir}{file} FILE_ID.DIZ ram: >nil:
endif
if exists {playpendir}{file}.exe
  exedescript x ram:file_id.diz {playpendir}{file}.exe >nil:
endif
if exists {playpendir}{file}.txt
  txtextract e ram:file_id.diz {playpendir}{file}.txt >nil:
endif
if exists {playpendir}{file}.zip
  unzip {playpendir}{file}.zip FILE_ID.DIZ >nil:
endif
if exists {playpendir}{file}.dms
  dmsextract ram:file_id.diz {playpendir}{file}.dms >nil:
endif
if exists ram:file_id.diz
  copy ram:file_id.diz {workdir}{file1}.DIZ
endif
