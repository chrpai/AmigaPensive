[Don't compare these strings!]
Area:
-------------------------------------------------------------------------------
-----------------------------------------------------------------------------
[NOWLW2
[MONMW95
Area
registered
generated

[Area headlines]
Area:
[NOWLW2
[MONMW95

[Wanted areas]
          // No areas=all areas

[String stop]
.             // One character, No characters=off

[Index 2 write]
a             // a or A=all lines, 1-9=number of lines

[Write headlines]
1             // b or b = both, 1 = index file 1, 2 = index file 2.

[Identical not saved]
n             // y or Y for yes

[Case not sensitive]
n             // y or Y for yes

[Starting column]
1             // Starting column of file names in index file 1
1             // Starting column of file names in index file 2
1             // Starting column of area headlines in index file 1
n             // Spaces are OK - b or B=both, 1=index 1, 2=index 2
n             // Tabulators are OK - b or B=both, 1=index 1, 2=index 2

[Date matching]
n        // b or B=both, 1=i1 and 2=i2, d or D or s or S=compare i1 with i2
24            // Starting column of date in index file 1
24            // Starting column of date in index file 2
b             // Index 1 & BBSSplit.ini, date format - see docs
b             // Index 2, date format - see docs
/             // Index 1 & BBSSplit.ini, date separator, don't use \
/             // Index 2, date separator, don't use \
01/01/96      // Index 1, don't allow files older than
01/01/97      // Index 1, don't allow files newer than
01/01/96      // Index 2, don't allow files older than
01/01/97      // Index 2, don't allow files newer than
