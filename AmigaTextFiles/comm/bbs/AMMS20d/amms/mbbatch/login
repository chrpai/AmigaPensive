; alle Breaktasten sind gesperrt !

_break on/c/x                                   ; Textabbruch anstellen

_on error/ignore                                ; Wenn Fehler, ignorieren

_disk text mbtext:login.txt                     ; Login-Text anzeigen

_login

_if {name} == "ZERBERUS" then _goto ZERBERUS	; wenn User=ZERBERUS, verzw.
_if {name} == "JANUS" then _goto JANUS		; wenn User=JANUS, verzw.
_if/p {name} == "NET@*" then _goto NET		; wenn NetzUser, verzw.

; globale Symbole (Abkuerzungen) definieren

WHO=="show ports"
BYE=="logout"
LOgoff=="logout"
ENde=="logout"
EXIT=="logout"
START=="echo ""Systemstart am {startupdate} um {startuptime} Uhr !"""
D=="dir/all"
DF=="dir/all/noboards"
DB=="dir/all/nofboards"
RESET=="echo ""#Termreset##invers#Terminalreset ausgefuehrt !#reset#"""

_break on/y/z                                   ; alle Breaks zulassen

_news
_execute batch                                  ; Userbatchdatei ausfuehren

_eod

JANUS:						; Janus/Zerberus login
ZERBERUS:
_execute batch
_logout/f

NET:						; Fastnet login
_Fastnet Server
_logout/f
