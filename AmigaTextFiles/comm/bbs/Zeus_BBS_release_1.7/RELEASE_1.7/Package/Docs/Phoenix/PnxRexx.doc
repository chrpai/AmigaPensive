TABLE OF CONTENTS

   L - Line command
   A - Async command (Can be used outside Zeus scripts)

[--] pnxrexx/
[LA] pnxrexx/PnxGetCompressStr
[LA] pnxrexx/PnxGetConfig
[LA] pnxrexx/PnxGetErrorMsg
[LA] pnxrexx/PnxGetExtractStr
[LA] pnxrexx/PnxGetFileArc
[LA] pnxrexx/PnxGetPnxConfig
[LA] pnxrexx/PnxIsHere
[L-] pnxrexx/PnxMaskArea
[LA] pnxrexx/PnxQuit
[L-] pnxrexx/PnxRemConfig
[LA] pnxrexx/PnxSetConfig
[LA] pnxrexx/PnxSetSchedule
[LA] pnxrexx/PnxStopExport

pnxrexx/                                                     pnxrexx/

   NAME

   SYNOPSIS

   FUNCTION

   NOTES

   INPUTS

   RESULT

   BUGS

   SEE ALSO

pnxrexx/PnxGetCompressStr                           pnxrexx/PnxGetCompressStr

   NAME
      PnxGetCompressStr

   SYNOPSIS
      PnxGetCompressStr <ArcNo>

   FUNCTION
      Returns the compress string of archiver <ArcNo>. In the string
      there should be a ~a. You should place the name of the archive
      to make here. There should also be a ~f. You should replace this
      with the file(s) to compress.

   NOTES

   INPUTS
      ArcNo    A number of an archiver in Zeus' configuration.

   RESULT
      RC  0 Ok, the compress string should be in RESULT.
      RC  5 Bad arguments.
      RC 11 Couldn't find archiver.

   BUGS

   SEE ALSO

pnxrexx/PnxGetConfig                                     pnxrexx/PnxGetConfig

   NAME
      PnxGetConfig

   SYNOPSIS
      PnxGetConfig <CHARSET|ARCHIVER|PKTTYPE|MAXAREA|MAXPKT|NEWS|HELLO|
                    GOODBYE|FILELIST|INDEX|FROMUSER|ALLNEW|BULLETINS|
                    WRAP|LASTPKTDL|NOSCHEDULER|ASYNCGRAB|TWIT> [user name]

   FUNCTION
      Returns information from user's Phoenix setings.

   NOTES
      If you launch it as an async command you must supply the name of the
      user. The name must be the last (second) argument.

   INPUTS
      CharSet     Obsolete
      Archiver    User's archiver
      PktType     User's packet format (QWK, MBBS etc)
      MaxArea     User's max msgs/area
      MaxPkt      User's max msgs/packet
      News        Include news file
      Hello       Include hello file
      Goodbye     Include goodbye file
      Filelist    Include file list
      Index       Create QWK .NDX files
      FromUser    Include msgs from the user
      AllNew      Messages will be dumped from high pointer, not last x.
      Bulletins   Include new bulletins since last logon
      Wrap        Where to wrap messages
      LastPktDl   When the user last downloaded a mail packet (N/A now)
      NoScheduler User shouldn't have access to the scheduler system
      AsyncGrab   Packet should be generated in the background

      UserName    Name of user if command is used as an async command.

   RESULT
      RC 0  Ok, requested info is in RESULT
      RC 5  Bad arguments
      RC 10 Couldn't get line
      RC 11 Couldn't load user
      RC 12 Couldn't load user's XData
      RC 13 Couldn't get user's Phoenix config (Use PnxSetConfig to
            create one)

   BUGS

   SEE ALSO
      PnxSetConfig PnxRemConfig

pnxrexx/PnxGetErrorMsg                                 pnxrexx/PnxGetErrorMsg

   NAME
      PnxGetErrorMsg

   SYNOPSIS
      PnxGetErrorMsg <error code>

   FUNCTION
      Returns the error message belonging to an error code/number.

   NOTES

   INPUTS
      error code  The error code

   RESULT
      RC  0 Ok, message is in RESULT
      RC  5 Bad arguments

   BUGS

   SEE ALSO

pnxrexx/PnxGetExtractStr                             pnxrexx/PnxGetExtractStr

   NAME
      PnxGetExtractStr

   SYNOPSIS
      PnxGetExtractStr <arcno>

   FUNCTION
      Returns the extract string of archiver <arcno>. In the string
      there should be a ~a. You should place the name of the file
      to extract here.

   NOTES

   INPUTS
      ArcNo    A number of an archiver in Zeus' configuration.

   RESULT
      RC  0 Ok, the extract string should be in RESULT.
      RC  5 Bad arguments.
      RC 11 Couldn't find archiver.

   BUGS

   SEE ALSO

pnxrexx/PnxGetFileArc                                   pnxrexx/PnxGetFileArc

   NAME
      PnxGetFileArc

   SYNOPSIS
      PnxGetFileArc <fullpath>

   FUNCTION
      Needs full path to a file. This function will then try to determind
      what archiver the file is archived with using Zeus' archiver
      configuration.

   NOTES

   INPUTS
      FullPath  Path to a file to check.

   RESULT
      RC  0 Ok, the archiver number is returned in RESULT.
      RC  5 Bad arguments.
      RC 11 Couldn't open the file.
      RC 12 Unknown file type.

   BUGS

   SEE ALSO
      PnxGetExtractStr PnxGetCompressStr

pnxrexx/PnxGetPnxConfig                               pnxrexx/PnxGetPnxConfig

   NAME
      PnxGetPnxConfig

   SYNOPSIS
      PnxGetPnxConfig <BBSID|PHONENO|HELLO|GOODBYE|NEWS|FILELIST|WORK|
                       MAXPKT|MAXAREA|ZONE|NET|NODE|POINT>

   FUNCTION
      Returns information from Phoenix's own configuration.

   NOTES

   INPUTS
      BbsId    BBS Id.
      PhoneNo  Phone number to the BBS.
      Hello    Full path to the hello file.
      Goodbye  Full path to the goodbye file.
      News     Full path to the news file.
      Filelist Full path to the new files list.
      Work     Work path.
      MaxPkt   Limit of how many messages that can be dumped pr packet.
      MaxArea  Limit of how many messages that can be dumped pr conference.
      Zone     BBS primary Zone number.
      Net      BBS primary Net number.
      Node     BBS primary Node number.
      Point    BBS primary Point number.

   RESULT
      RC  0 The requested information is in RESULT.
      RC  5 Bad arguments
      RC 11 Couldn't load Phoenix's configuration.

   BUGS

   SEE ALSO

pnxrexx/PnxIsHere                                           pnxrexx/PnxIsHere

   NAME
      PnxIsHere

   SYNOPSIS
      PnxIsHere

   FUNCTION
      Checks if Phoenix is running.

   NOTES

   INPUTS

   RESULT
      RC  0 - Phoenix is running.
      RC 20 - Phoenix is not running.

   BUGS

   SEE ALSO
      PnxQuit

pnxrexx/PnxMaskArea                                       pnxrexx/PnxMaskArea

   NAME
      PnxMaskArea

   SYNOPSIS
      PnxMaskArea <area> [GET|IN|OUT]

   FUNCTION
      Masks in or out an area, or tells if an area is masked out (resigned).
      If RESULT is 1 the area is masked out.

   NOTES

   INPUTS
      area  Number of area.
      GET   Is area masked out?
      IN    Mask area in (join).
      OUT   Mask area out (resign).

   RESULT
      RC  0 Ok/Info is in RESULT.
      RC  1 Mask in/out failed.
      RC  5 Bad arguments
      RC 10 Couldn't get line.
      RC 11 Area doesn't exists.

   BUGS

   SEE ALSO

pnxrexx/PnxQuit                                               pnxrexx/PnxQuit

   NAME
      PnxQuit

   SYNOPSIS
      PnxQuit

   FUNCTION
      Will try to shutdown the Phoenix Arexx/CC manager.

   NOTES

   INPUTS

   RESULT
      RC 0  - Ok
      RC 10 - Failed

   BUGS

   SEE ALSO
      PnxIsHere

pnxrexx/PnxRemConfig                                     pnxrexx/PnxRemConfig

   NAME
      PnxRemConfig

   SYNOPSIS
      PnxRemConfig

   FUNCTION
      Will delete the user's Phoenix configuration.

   NOTES
      To remove the area configuration you can use "PnxMaskArea RemAll".

   INPUTS

   RESULT
      RC  0 - Configuration found and removed.
      RC 10 - Couldn't find the configuration (It's allready removed).

   BUGS

   SEE ALSO

pnxrexx/PnxSetConfig                                     pnxrexx/PnxSetConfig

   NAME
      PnxSetConfig

   SYNOPSIS
      PnxSetConfig [USER <user name>] [CHARSET <data>] [ARCHIVER <data>]
                   [PKTTYPE <data>] [MAXAREA <data>] [MAXPKT <data>]
                   [NEWS <data>] [HELLO <data>] [GOODBYE <data>]
                   [FILELIST <data>] [INDEX <data>] [FROMUSER <data>]
                   [ALLNEW <data>] [BULLETINS <data>] [WRAP <data>]
                   [LASTPKTDL] [NOSCHEDULER <data>] [ASYNCGRAB <data>]
                   [TWIT <data>]

   FUNCTION
      Will set some data in the user's Phoenix configuration. If the
      user didn't have a configuration it will have now.

   NOTES
      This function scans all your arguments and parses them. I.e. you
      can set several fields in just one call.

      If used as an async command you *must* supply the 'USER' keyword
      and user name before any other arguments.

   INPUTS
      UserName    Name of user if command is used as an async command.
                  MUST be the first argument.

      CharSet     Obsolete
      Archiver    User's archiver
      PktType     User's packet format (0 QWK, 1 Bluewave, 2 MBBS)
      MaxArea     User's max msgs/area
      MaxPkt      User's max msgs/packet
      News        Include news file
      Hello       Include hello file
      Goodbye     Include goodbye file
      Filelist    Include file list
      Index       Create QWK .NDX files
      FromUser    Include msgs from the user
      AllNew      Messages will be dumped from high pointer, not last x.
      Bulletins   Include new bulletins since last logon
      Wrap        Where to wrap messages
      LastPktDl   Sets when the user last downloaded a mail packet to
                  current time.
      NoScheduler User shouldn't have access to the scheduler system
      AsyncGrab   Packet should be generated in the background

   RESULT
      RC  0 Ok
      RC  5 Bad arguments
      RC 10 Couldn't get line
      RC 11 Couldn't load user
      RC 12 Couldn't load user's XData
      RC 13 Couldn't set twit filter
      RC 14 Couldn't add new configuration
      RC 15 Couldn't save configuration

   BUGS

   SEE ALSO
      PnxGetConfig PnxRemConfig

pnxrexx/PnxSetSchedule                                 pnxrexx/PnxSetSchedule

   NAME
      PnxSetSchedule

   SYNOPSIS
      PnxSetSchedule <[ADD <day> <hour> <min>] | [DROP <day>]> <user name>

   FUNCTION
      Adds or drops a weekday the scheduler should automatically make
      a mail packet for ä user.

   NOTES

   INPUTS
      ADD      Add a weekday.
      DROP     Drop a weekday.
      day      A weekday (Monday = 0, Tuesday = 1 etc)
      hour     Hours after midnight.
      min      Minutes after the hour.
      UserName Name of user.

   RESULT
      RC  0 Ok
      RC  5 Bad arguments
      RC 11 Couldn't load user
      RC 12 Couldn't get user's Phoenix config
      RC 13 User isn't allowed to use the scheduler system.
      RC 14 Out of memory

   BUGS

   SEE ALSO

pnxrexx/PnxStopExport                                   pnxrexx/PnxStopExport

   NAME
      PnxStopExport

   SYNOPSIS
      PnxStopExport <user name> [CANCEL]

   FUNCTION
      Aborts or stops an async grab. If the CANCEL argument isn't given
      the mail packet will be generated, however no more messages will be
      dumped.

   NOTES

   INPUTS
      UserName    Name of user, which mail packet should be stoped.
      CANCEL      Completly abort the package.

   RESULT
      RC  0 Exporter found, and aborted.
      RC  5 Bad arguments.
      RC 11 Exporter not found.

   BUGS

   SEE ALSO

