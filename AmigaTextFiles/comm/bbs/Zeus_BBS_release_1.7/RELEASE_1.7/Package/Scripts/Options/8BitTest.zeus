/*
** 8BitTest.pl -- Asks the user if they can see 8 bit characters
*/

Options Results
Parse Arg Line

ClrUserFlag 'No8Bit'

Display 'Options/8BitTest'

Read Opts '"12"' MinLen 1 MaxLen 1 IsMenu
if RC ~= 0 then exit

if RESULT = '2' then SetUserFlag 'No8Bit'
