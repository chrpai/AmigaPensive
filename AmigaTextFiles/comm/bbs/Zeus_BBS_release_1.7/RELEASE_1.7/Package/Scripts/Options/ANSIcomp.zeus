/*
** ANSIcomp.pl -- Asks the user if their terminal supports ANSI compression
*/

Options Results
Parse Arg Line

SetUserFlag 'ANSIcomp'

Display 'Options/ANSIcomp'

Read IsMenu Opts '"12"' MinLen 1 MaxLen 1
if RC ~= 0 then exit
Choice = RESULT

if Choice = '2' then ClrUserFlag 'ANSIcomp'
