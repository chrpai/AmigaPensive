/*
** ANSIpos.pl -- Asks the user if their terminal supports ANSI positioning
*/

Options Results
Parse Arg Line

SetUserFlag 'ANSIpos'

Display 'Options/ANSIpos'

Read IsMenu Opts '"12"' MinLen 1 MaxLen 1
if RC ~= 0 then exit
Choice = RESULT

if Choice = '2' then ClrUserFlag 'ANSIpos'
