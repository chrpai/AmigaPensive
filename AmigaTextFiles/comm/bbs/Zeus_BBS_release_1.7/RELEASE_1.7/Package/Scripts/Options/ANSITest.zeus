/*
** ANSITest.pl -- Asks the user if they can see ANSI colours
*/

Options Results
Parse Arg Line

ClrUserFlag 'NoANSI'

Display 'Options/ANSITest'

Read IsMenu Opts '"12"' MinLen 1 MaxLen 1
if RC ~= 0 then exit
Choice = RESULT

if Choice = '2' then SetUserFlag 'NoANSI'
