/*
** Gender.pl -- Set users gender
*/

Options Results
Parse Arg Line

Display 'Options/Gender'

Write '" "'
Read Opts '"012"' IsMenu MinLen 1 MaxLen 1
if RC ~= 0 then exit
Choice = RESULT

SetUserData 'Gender' Choice
