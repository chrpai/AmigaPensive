Appendix 7:


                    Required Workbench and System Files



           REQUIRED FILES FOR A BOOT-ABLE DISK CONTAINING 4D-BBS
					(for Floppy drive users)

The C directory:
	assign
	cd
	copy
	delete
	dir
	execute
	ff			(Not needed if you're running under WB 2.0)
	join
	makedir
	rename
	run
	stack
	<your favorite text editor>
	4D-BBS		(Supplied by 4D-BBS disk)
	ReceiveZ or rz	(For File Transfers)
	SendZ	 or sz	(For File Transfers)

The DEVS directory:
	serial.device
	system-configuration
	mountlist

The FONTS directory:
	4D-BBS.font				(Supplied by 4D-BBS disk)
	4D-BBS (directory)
		8

The L directory:
	Disk-Validator
	Port-Handler
	Ram-Handler

The LIBS directory:
	diskfont.library
	mathtrans.library
	mathieeedoubbas.library
	mathieedoubtrans.library

The S directory:
	startup-sequence		(Supplied by 4D-BBS disk)
