The files in the Include/Excel directory are assembly conversions of the
C include files for Excelsior!.  I split them into different files to
make for more efficent usage.

Normal usage is just to include the file Excel.i, which loads all the
other files.
