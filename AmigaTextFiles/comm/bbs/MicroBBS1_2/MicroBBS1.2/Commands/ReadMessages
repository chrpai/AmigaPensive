.key USER/A
.bra {
.ket }

IF EXISTS "BBS:Messages/{USER}"
    C:Ask "*nNachricht loeschen (y/N)?"

    IF WARN
        C:Delete >NIL: "BBS:Messages/{USER}"
    ENDIF
ELSE
    C:Echo "Keine Nachrichten gefunden."
ENDIF
