; Denne filen gir deg mulighen å nekte folk å bruke visse filnavn.
; Denne fila tar standard amigados wildcards
; Slik fila er nå, kan ingen rename, upload'e osv noen fil som slutter
; på .zip eller "testfile"

testfile
#?.zip
