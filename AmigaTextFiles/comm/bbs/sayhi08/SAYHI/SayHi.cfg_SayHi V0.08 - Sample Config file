1  ;LINE1-minimum level needed for user to read here. (def.1) (rg.1-9)
2  ;LINE2-minimum level needed for user to write here. (def.2) (rg.1-9)
3  ;LINE3-number of lines a user can add per day. (def.5) (rg.1-50)
15 ;LINE4-maximum number of entries: SayHi file. (def.20) (rg.5-500)
;=====anything on this line or below is ignored but make sure if you use
a config file at all that ALL the above lines are there in precise order!
The comments appended to each line of the cfg file can be left in as long
as they are found AFTER a semi-colon!  If any lines are commented, blank,
or the values entered outside the given range, defaults will be used.

