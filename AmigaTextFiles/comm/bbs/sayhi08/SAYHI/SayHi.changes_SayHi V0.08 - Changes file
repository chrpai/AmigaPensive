     ==============================================
      Changes and additions to note in SAYHI V0.08
     ==============================================

Local cursor turned on and off where necessary seeing a nice 
speedup in the output everywhere! Thanks Pete. (Also Kevin
mentioned this to me as well. Taa.)

Compressed spaces in the main menu display by using SPACE$(n) and
adjusted a few delays and outputs here and there to help smooth
program function even more.

Fixed the time expired check to quit rather than just sit there.
Thankfully I found it myself rather than a user on the BBS, as
they'd be in the door, time expired and a 1-line editor waiting.
Generally tidied up the timewarn code somewhat. Looks fine now.
[I'll also have to fix this in GMINER, I-Ching and my template
list as its a rather nasty one from a BBS point of view!]

Moved the deleting of the workfiles in Ram: into SayHi's 'exit'
routine instead of after each SayHi line is added! Much better.

Ensured that ANSI attributes were turned off after main menu,
(it was actually), the info screens, and after displaying the
sayhi.dat file to the user.

                                     06-Jan-95  PKN
