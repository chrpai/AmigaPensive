;;;  Mount CDrom drive at the moment it is needed so it doesn't
;;;  spin the whole time and it doesn't need (buffer) memory

postmount 6    >nil:
mount cd0:     >nil:
wait 2 secs    >nil:

assign  CD1:    CD0:
assign  CD2:    dh2:CD       ;CD rom emu for testing purpose

