Use the DIR command to display a list of download directories to which you
have access.  The command will list the directory's name and a brief 
description.  You use this name to select a current directory via CD or an
equivalent.
