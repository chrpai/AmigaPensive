failat 11

; An Optional way of playing a sample when the chat script is invoked.
; Replace it with whatever you want or just leave it commented.

;	run >NIL: C:PLAY SAMPLES:CHAT.IFF

DLG:chat -t
if warn

; Following line must *NOT* be changed... Pager will not work without it!
  echo >T:NoChat ""

  Quit
endif
