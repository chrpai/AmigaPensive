.KEY NEWN,PATHF,FN
.BRA {
.KET }
echo "{PATHF} {NEWN} {FN}"

cd ram:
makedir {FN}
cd ram:{FN}
changetaskpri -1
unzip -d -o -x "{PATHF}"
protect #? +rwed all
lha -m -x -a -r -X -2 a "{NEWN}" ram:{FN}/#? s:Z_MC.Displayme
;	     Edit the above path filename   ^^^^^Edit this^^
cd ram:
delete ram:{FN} ALL
