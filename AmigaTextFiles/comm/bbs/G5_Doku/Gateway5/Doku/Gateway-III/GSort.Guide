@database Gateway-5.guide

## This is a doc file in the amigaguide.library format.
##
## This guide is written for Gateway-5 by Michaela Prüß
##
## $VER: Version 5.2 vom 26.03.1999
##

@Node Main "GSort"

Gateway-5 Guide          GSort                         26.03.1999

GSort ist der Dateisorter von Gateway-5. Es handelt sich um einen
extrem schnellen und sehr flexiblen Sorter.

GSort wird von Gateway-5 intern aufgerufen. Das Programm kann aber
auch extern als Sort-Funktion benutzt werden. Es muß wie folgt gestartet
werden:

GSort <datei> <satzlaenge> <beginn> <laenge> [1]

<datei>      ist die Datei, die sortiert werden soll.
<satzlaenge> ist die Datensatzlänge pro Satz incl. abschießendem CR.
<beginn>     ist die Stringposition, ab der der Sortierbegriff über
             den die Sortierung erfolgen soll im Datensatz steht.
             Dabei zählt das Programm C-typisch von 0 bis <satzlaenge>-1!
<laenge>     ist die Laenge des Sortierbegriffes, über den sortiert werden
             soll.
[1]          1 = Datumsort (TT.MM.JJJJSS:mm:ss => JJJJ.MM.TTSS:mm:ss)

Beispiel:

Sortierung der Befehlsliste: Gsort System/Befehle 221 1 10

Linksbündige Leerzeichen werden bei der Sortierung entfernt und der ange-
gebene Sortierbegriff wird rechtsbündig verwendet!

@ENDNODE
