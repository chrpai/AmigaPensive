
Usage: /LOOKUP "<first> <last>"
   Looks  up  a  user's registry.  It displays their real name, alias,
   last time they changed their entry, interests, etc.

   Example:
      /LOOKUP "John Smith"

   Note:
      TeleReg database must be installed to work.

