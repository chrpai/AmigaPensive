/*  Random Chat Prompts    */

options results;tr=transmit;sf=sendfile;se=sendstring


  call open list, 'systext:chat/list','R'
    max=readln(list)
  call close list
  ran=random(1,max,time(s))
    sf 'systext:chat/'ran
      exit