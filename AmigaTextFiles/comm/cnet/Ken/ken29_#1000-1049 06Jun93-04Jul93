
Sunday 06-Jun-93 23:55:41

--1000--

HH> BA> Can't you nest things more than 1-2 levels deep?

HH> BA> 200 subs, each with 200 subs, each with 200 bases.

That was MY recommendation, not a quote from you.

HH> Well I did not say anything about  200 subs within  200 subs I said I have
HH> REC that has 1,200 areas and I want them all in the REC so inside REC
HH> there woulod be 1,200 areas or newsgoups not a bunch sub-dir levels I want
HH> like 14 areas with all subs for each area in that dir.

So you DO WANT 1 sub with 1200 bases in it????
I would think that would make it harder to find things.
"Hit L, wait a week for a mile-long list (25 screen fulls) to scroll by."
(Think of 2400bps users!)

But break it down into 14 subs, put 85 bases in each and you've killed 2 birds
with 1 stone:
1)  CNet will let you do it.
2)  User's never get dumped a list of more than 85 bases at 1 time.

Randy, on a slightly different, but related matter...

What would be your (or anyone's) suggestions for fast/easy creation of 500
Fidonet or 1000 UUCP subs?

Maybe some kind of 'bulk-subboard creater' executable?

You feed it a textfile list of subboard names, and a physical subboard # to
copy...

>BulkSub MyList.txt 217

If MyList.txt contains 500 lines, it makes 500 new sub entries.

Maybe even run it on-the-fly, to create new subs as newsgroups are
added/dropped, automatically.

--1001--

One more thing regarding the "start-time" and "valid for" time....

Is there a way (or is 1 planned) to have an event "valid for" the
amount of time the task actually took to complete?

That is...
> Run this task, don't answer the phone, until that task is completed.

Instead of the current...
> Run this task, don't answer the phone for 15 mins, regardless if the
> task took 1 min, 14 mins, or 25 mins to complete.

If we have to guess at an "approximate completing time", we have to
always assume the max.

Maybe have a "valid for" time of 0 or -1, mean "until task is done and
returns".

I would set ALL of my "must be off-line" tasks to this setting.

--1002--

> 221.   Two new "range" commands were added to make using a subboard even
>  more powerful.  These are added to commands like Read and Scan
>     BY {username|account#}
>     TO {username|account#}

I assume that "username" only works for users that are specifically
a member of that BBS.

It would be great to search through 1000s of Fidonet msgs, imported
from remote systems, for users that are not members of your BBS.

Possible?  Slow?

--1003--

KEN> 219.   Subboards may now select an "auto-free after days" value.  Files
KEN>  will automatically become "free downloads" this number of days
KEN>  after they have been uploaded.  Using the AT command, it's now

I love this feature.  Anyway to keep it, and add a slight twist...

BA> If this "auto-free after days" value is negative, then it means:
BA> "Files will automatically become "free downloads" this number of days
BA> BEFORE they are going to be PURGED."

Heck, I'd gladly set ALL my files to "become free" 5 days before they
are about to be killed.  Might as well give them away to someone if
they are about to be deleted off my HD forever, soon.

--1004--

Ken, are we eventually (before v3.0) going to be able specify a path
for the speller's dict* files?  At least get them out of ram's root
path.  At least consider the much faster (and rebootable) rad: or
other ram:disks.

A new CONFIG/PATH?
Or maybe share 1 of its current 7 path settings?

I eventually hope to have a 300K-500K dict file.  Don't know if
it'll be worth keeping all that on a ram:disk 24hrs/day for the very
few users that will need to do spell-checking.  (But I DO want it
available to them.)

Or maybe different paths for the giant 150K-500K dict, and the small
<1K index file?  Which 1 (or both) really need the speed advantage of
the current "forced ram:disk storage only"?

--1005--

JR>         1.  How about ensuring that when cnet transforms files that it
JR> will
JR>             delete the old files no matter what thier protection bit is
JR> set
JR>             to!!

Thre are MANY things that can cause CNet's default transfer scripts to fail.
They should always...

Use double quotes everywhere.
Makedir dirs that are guaranteed to be unique.
Allow for extracting/deleting filenames that may have the 'protect-bit' set.
Check for available space before extracting.
Extract to different dirs for multi-lined systems.
Allow for archivers that extract to full sub/sub/sub/subdir names.
Check that makedir isn't trying to make a dir that already exists.
Delete all the extracted files even if the caller drops carrier.
Shut off archiver-options that prompt the user "overwrite this file?".
Don't use archivers that crash/GURU on badly corrupted files.

K> Another way to do that would to put the protect {pathf} #? +r +w +e (etc
K> etc ) just have the script re-dot the protection on the files.

In any case, make sure the script re-sets those flags back to their original
settings afterwards.  Some authors specifically set 's' flags for 'scripts,
etc.  (And these should remain set like that in your final transformed file.)

--1006--

At the respond/pass prompt it would be handy to read responses
"2,6,10" by entering "2,6,10".  (I can quickly SCAN and find the
ones I want to read, but how do I quickly read them?)

Hitting "?" at this prompt doesn't mention anything about ranges, in
fact, it doesn't mention anything at all about "response-reading by #":
> n      : Read response #n

--1007--

> Yank-new again...

Now I'm, once again, lost and confused.  So I'll post on how I "think" this
should work.  (This may or may NOT be how it actually should/could/does work.)

I'll use the words "successful" and "unsuccessful" in reference to
whether or not the user hit his "max-yank-size-limit".

1)  A user successfully yanks everything in 1 shot. No problems.
2)  A user unsuccessfully yanks, and has to yank "in pieces".  Each successful
yank updates the per-sub pointers so those subs aren't yanked twice.  YA YA YA
would get bundles of 300K 300K 143K. (That's on a system with a 300K limit,
if a total of 743K needs to be yanked, overall).

No calling back.  (If you can get a line and have the $$$, and time.)
No Move-to-main/RELOG/Name/Password.  (If you have knowledge of it and
access to it.)

--1008--

TB> I reloaded the bbstext/bbsmenu a minute ago while I and another user was
TB> on the system.. and all I'd changed was bbsmenu.  Suddenly, his 'Where'
TB> string was set to "X", and the text in the title bar of the X-fer window
TB> that was up on his port was entitled.. well, I can't seem to reproduce
TB> those particular symbols. :)

Are users online still doing things while this bbstext/bbsmenu is being
loaded?  Maybe catching it 1/2 way through the load?  CNet should 'halt'
everything until the full bbstext/bbsmenu has been loaded.

(Sure wish that pull-down menu said "Text/Menu reload" instead of just
"Text re-load".  I didn't even realize that CNET had "reload BBSMENU"
powers.)

It's a great new feature.

--1009--

Ken, weren't "Default byte credits" and "Default file credits" a per-
access-group variable before?

And they're now under a global setting under CONFIG/DEFAULTS.

How would I do something like:
 Level 10, 300K and 20 files.
 Level 11, 600K and 40 files.
 Level 12, 900K and 60 files.
 etc...

Without 'by-hand' editing the user accounts AFTER they've all gotten
just the global default setting.

--1010--

Ken, please consider creating a new VDEentry "type".

It would allow displaying/editing of just "date" instead of the current
"date/time".

There are quite a few IsData fields that don't really need the current
"per-minute" accuracy.  Do we really need to specify the exact hour/
minute to purge-files, expire-accts, purge-accts, g/n/pfile purge-
times, and other tasks that are only going to be done by amaint at a
preset time.  The dat-mon-yr would be plenty.

And fields like "birthdate", etc, where the user can't even set
the hr:mn.  (Nor should it be added.)

During VDE editing, entering " 00:00" wouldn't be needed.  (Currently,
if you don't specify hr:mn, CNet rejects the whole entry.)

If you don't feel this is a good default for CNet, then just making the
new VDE DATE 'type' would allow us to modify VDE.c ourselves.  (Handy
for squeezing more info into crowded VDE displays, too.  Cuts down on
'visual-clutter', too.)

--1011--

> I notice a tendency for people using QWK readers with CNet to
> quote ENTIRE long message segments before making a one-line reply,
> apparently not THINKING about the fact that this gigantic mess they are
> creating gets APPENDED right on the end, creating an annoying duplication.

I'm NOT going to defend users that over-quote, but I guess reading/
replying offline makes one 'forget' about what it'll look like later,
online.

And I'm NOT going to blame CNet, but for non-QWK users, the line-
editor does an excellent job of cutting down on over-quoting,
automatically.  You have to specifically tell it to quote which lines.

The full-screen editor does exactly the opposite.  It quotes the full
text, and you have to specifically cut it down yourself.  (Some users
just don't bother to do so.)  Maybe the latter could be more like the
former.

I'd love to see some figures of which users over-quote the most:
1)  QWK users
2)  Line-editor users
3)  Full-screen-editor users.

--1012--

When "Inviting" new users into an invitation-only sub...

In this case, the user's ID# is 492.
The sub's are 4,5,6.

1) Sear's Good:
   Move into each sub, 1-by-1, invite that 1 user.
   > 4
   > INVITE 492
   > 5
   > INVITE 492
   > 6
   > INVITE 492

2) Sear's Better:
   No need to move into that sub.
   Just (optionally) specify the sub #, and invite that 1 user.
   > INVITE 492 4
   > INVITE 492 5
   > INVITE 492 6

3) Sear's Best:
   No need to move into that sub.
   No need to specify each sub 1-by-1.
   Just (optionally) specify the RANGE of sub #s, and invite that 1 user.
   > INVITE 492 4-10

(CNet currently does #1.)

--1013--

JR>         2.  On many an IBM board if you download a file you are likely to
JR>             find a file called file_id.diz inside of the archive.  All
JR> this

I'd like to see a file included with every batch DL that would contain:
> FileNote FileName1.LHA "The online short file note..."
> FileNote FileName1.LHA "The online short file note..."
> FileNote FileName1.LHA "The online short file note..."

After you finished DLing, you'd c:execute it, all your DLs are instantly,
accurately filenoted.

L> I'm still confused where the filenotes on the files are actually lost! I
L> suggested to Ken to check to see if there was a way for the filenote to
L> stay attached to a file during a transfer. Any idea where it's lost Bill?

That DOS filenote isn't as "attached" to that file as you think.  AmigaDOS
knows it is there, but how do we tell the receiving terminal?

The first handful of bytes during a transfer is where the receiving computer
gets info about the tranfer.  The file's size, date, filename, etc.
But now you are asking it to also "send this short note, too".
I'm not sure if Zmodem could be expanded to do this or not.
Even if it could, only those BBSes and Callers that used this one 'special
zmodem' could use this feature.  Very limiting.

But, the other idea of sending a small DOS script would work with any/all
past/present/future batch protocols, all BBSes, all terms.

--1014--

Hey, this item is getting to be a "post your menu suggestions here".

Maybe "100 alternate menus" would be a great 'idea-starter' to include
with CNet.  (hint, hint, hint)

--1015--

I'd love to see a new CNET EVENT type:
> CNET-CMD <acct args>
It would place you under acct #x, at CNet's main menu.
The remaining args would be whatever keystrokes you'd type (not just
'all-prompt cmds) if online.
> CNET-CMD <1 u;sg!;o!>

And we'd new "open/close buffer" MCI cmds or BBSMENU cmds.

We could create a new system-event that would:
1> Open a buffer
2> UDBASE;Scan Global!;O!
3> Close/save buffer

Of course, step #2 could be any one of 1000s combos of Browse, Scan,
Search, Read, Yank, new, free, favorite, by {user}, to {user}, mess,
private, reverse, since, until, 'text-searches', unval, all, find, Z,
etc.  Jump in/out of certain subs.  ANYTHING!

Create (compressed-DL'able or non-compressed-readable) lists of:
> "All files online" or "all files x days old" or "all FREE files".
> Compile voting results, move them into easy-to-read gfiles.
> The output of any pfile, now saved to disk.
> The output of any CNet BBSMENU, now saved to disk.
> Automated 'guided-tours' of the BBS.
> Dump your mail to the printer each x hours.

Do any maintenance tasks every x days:
> Kill all files, in all areas, that are x months old.
  (Without changing any sub's or any file's 'purge-days' settings.)
> Search/Yank msgs (and AO) for archival DLing by all users.
> Post "rules text" each x weeks, to Fidonet echos that you moderate.

Anything that you currently find yourself MANUALLY doing
each x days by:
> Logging in
> Opening a buffer
> Executing these cmds
> Closing/saving buffer
All could be fully automated with a simple CNet event.

--1016--

FW> Main> find 277_d1.dms
FW>
FW>   1 277_d1.dms       388K  2.x BETA TESTERS ONLY
FW>                            Many bug fixes ... be sure to see the
FW>                            README! on disk 2, there is a small upda...
FW>   2 277_d1.dms       388K  2.x BETA TESTERS ONLY
FW>                            Many bug fixes ... be sure to see the
FW>                            README! on disk 2, there is a small upda...
FW>
FW> FIND: Again, Download, *, Examine, Grab, Quit, [Pass]:

I often see FIND giving duplicates like this, on other systems or mine.

Running "make_allfiles" fixes it, but is there a way to "prevent" it
in the first place?

What's causing it?

--1017--

P> I have been using the fifo.libaray that someone uploaded to me.
P> Its 4256 bytes. The one on the disk 2 is 412 bytes.

Yikes.  That's quite a decrease in size.  1 of smallest libraries I've ever
seen.

Ken, if you could toss a version # in fifo.library, it would help us greatly
in the identification process.  (As well as the installation/update process.)

J> LH> Hi Ken... I started using the Fifo lib from Fish 588 and so far it

J> That is the version I am using too and that I've uploaded on FW about 2 or
J> 3 weeks ago. Len is right, it is working perfect !

What's this file called?  I did a "FIND *fif*" and only saw the old version
that I UL'ed:

> Dillon's FIFO pipe v37.4 07-Dec-91

Personally, I'd always like to run the "Ken compiled" version.  For any custom
CNet benefits, latest bug fixes, new features, as well as to keep beta-testing
consistent.

Ken, did we miss anything new/fixed by NOT getting that lib update?
> fifo.library.277             412 ----rwed 10-Jun-93 02:36:11

--1018--

Under v2.77, when I "AO" CNet always uses signature #0 instead of the
correct one:
> "* 7.  Upload descriptions/handles"
> "* 8.  Upload descriptions/real names"

(Or should/could CNet have a special/separate "AO signature" in CONFIG?)

More on "wrong signatures when AO".  Everything below was
tested on v2.78, on a sub that was:
1 anonymous:     NO
2 aliases:       NO
3 no signatures: NO
All "AO" was done "by-hand", NOT via amaint.

0.  Default (others override)
1.  Local mail and feedback
2.  UUCP messages
3.  FIDO messages/handles
4.  FIDO messages/real names
5.  Other messages/handles
6.  Other messages/real names
7.  Upload descriptions/handles
8.  Upload descriptions/real names

In all cases signature #8 SHOULD have been used.

Signatures defined:          Signature used when AO'ing:
0,3,7,8                               0
3,7,8                                none
7,8                                  none

(Perhaps, it might be better to just have NO signature during 'by-hand'
"AO" and/or amaint "AO" regardless of which signatures are/aren't defined.)

--1019--

Regarding a custom CNet frontdoor...
S> This has been argued many times.  Ken doesn't have the time to put into
S> this.

Has KEN publicly stated this?

S> The current Trapdoor setup works great for everybody.

Great?
For EVERYONE?
I can't even get TD to do the simplest of things...
 Open/close its windows when *I* say so and they aren't needed.
 Re-size those windows when *I* say so.
 Stop running TD after control has been passed to CNet.
 Do NOT exit CNet after control is passed back to TD.

S> That has been argued extensively too.  A simple public screen option to
S> use
S> with your favorite term program is all that is needed.  Again, more time
S> distracted from Cnet programming for Ken

PUBLICSREEN support was added to CNet as of v2.40e 30-October-1992.
To the best of my knowledge NO ONE has ever got it to work at all.

Some people will see "great new features" as "that distracts Ken from
Cnet programming" others will see it as "great new features *ARE* Cnet
programming".

--1020--

xfido version "CNet 2.77 >>ALPHA 3.0<<"
is marking outgoing msg with:
> --- CNet XFIDO 2.75

(I couldn't figure out where by 'update/install' process was going wrong.)

--1021--

Under v2.78....

I've moved my dict* files and CONFIG/PATH to a non-ram: place.
I can spell-check fine.
But when I try and "EP;16" it says "speller not installed".
(Is it still looking for dict* in ram:)

--1022--

xfido is exporting all [K]illed items.

Also, a new bug....
1) Post a few responses.
2) Run RR for that sub.
3) See that the response still exists.  (as it should)
4) Run Xfido.
5) Nothing gets exported.

Ken, was the update for v2.78 suppose to include a new xfido, too?

(I'm using TrapToss and CONFIG/Fidonet set to "1.msg==highwater".
I have NO idea if that's where it should be set.)

--1023--

The last few CNet releases having been locking-up at 'log-out' about 5%
of the time.  Left in "ram:" is:

 shell1> failat >NIL: 21
 shell1> Run >nil: <nil: CNet:s/RenamePL 1
 shell1> endcli >NIL:

My "save-old-port DOS-script is":
 RenamePL> .key which
 RenamePL> .bra {
 RenamePL> .ket }
 RenamePL>
 RenamePL> FailAt 21
 RenamePL> Copy sysData:log/Port{which} sysData:log/OldPort{which}
 RenamePL>

My BBSTEXT has been modified to:
 BBSTEXT> @ Logoff complete n1 #4 Run >nil: <nil: CNet:s/RenamePL %23}

Any ideas?
Maybe shell1 is trying to ENDCLI before the background is done "run"ing?

--1024--

I'm NOT saying my set-up is correct, but if you ever want to see what
another sysop is using to config CNet/TrapToss/TrapDoor/etc, just dial
313-473-2020, log-in under acct "DEMO", pswd "DEMO", and go to the
Gfile menu:

>  1) USR HST-DS 14400 ROM Checksum
>  2) USR HST-DS 14400 RAM Test
>  3) USR HST-DS 14400 Current Call Time
>  4) USR HST-DS 14400 Current Settings
>  5) USR HST-DS 14400 Non-Volatile RAM
>  6) USR HST-DS 14400 Link Diagnostics
>  7) USR HST-DS 14400 Product Config
>  8) USR HST-DS 14400 Product Code
>  9) View Mail:Fido.cfg file
> 10) View Mail:TrapDoor.cfg file
> 11) Current Fidonet NodeList in use.
> 12) Nodelist Search
> 13) Full schedule of events
> 14) Version #s of various "CNet 2.78" executables
> 15) CNet modem configs (CNet:BBSport#)

NOT all of the above are active yet, but should be soon.

(Sure wish more sysops would make things like this available.)

--1025--

Something appears to be related to an item's flags...
At the respond/pass prompt, I can "AT" "EXIT" "AT" "EXIT" "AT" "EXIT"
and watch certain flags toggle on/off.  (That's w/o SAVING or EDITing any of
the VDE fields.)  "Integrity Check" toggles through each of its states each
time I "AT" and just "EXIT".

Am I running the wrong/old files?
>bmaint                     21540 ----rwed 14-Jun-93 15:27:08
>CNet 2.78 >>ALPHA 3.0<<

>subboard-item               2788 ----rwed 10-Jun-93 02:33:33
>Could not find version information

Maybe some of the other flags (that aren't shown on the VDE) are also being
toggled.  (Like anonymous, alias, etc.)

BA> Can someone else try the above?

Ken> I did, and found that bug, thanks.  You're not (completely) crazy!  Still
Ken> can't find a problem with the AO.
Ken> Ken

BB> I did, and found that bug, thanks.

Is it a VDE data file fix?  If so, can we get a quickie replacement?  I'm
concerned that the bug is flipping 100s of different flags on files that I
"AT".

BB> You're not (completely) crazy!

That remains to be seen.

BB> Still can't find a problem with the AO.

Ken, this might have been my fault.  I was flipping the same "wrong flags"
that the CNet bug was.

Please don't spend too much time looking for the AO bug until I can try the
VDE fix.  I am nearly certain that 1 will fix the other.

--1026--

Did anything change in the last 1-2 updates effecting the full-screen editor's
"insert or delete character" methods?

Just seems slower than before.  (While watching the whole line shift
left/right each character.)

--1027--

Can this be shown to users at log-in, too?

> Note: your mail box is FULL. You must KILL one or more messages in
> order to again receive mail.

You could end-up with a full mailbox for ages and never even know it.

--1028--

Ken, I'm having trouble with these 2 fields in VDEentry...

   short screen0, /* belongs-to screen */
      screen2;    /* goes-to screen, -1 for exit */

10,-1 Save    -|
               |--------|exit 0,-10
10,-2 No Save -|        |
                    0,0 |a
                    0,0 |b
                    0,0 |c      |exit     8,-10
                    0,8 |d>>----|previous 8,0
                    0,0 |e      |x 8,8
                    0,0 |f      |y 8,8
                                |z 8,8


When do screen0/screen2 become -1, -2, -10, -20?
Do subscreens have to be 1,2,3,4?  Or is 10,20,30,40 OK?

When I jump to my subscreens and move the cursor around, text from
the parent screen appears, too.

I notice other values in VDEentry must be "0" or "-1" at times.
When?

I'd love to know how it all works.

--1029--

Ken, regarding CNet's 10-line cmd-line 'history' buffer...

Please consider having it NOT place duplicate lines into the buffer.

Like the AmigaDOS shell, this will "appear" to extend its capacity.

If the user executes "FOO" 6 times in a row, just have it appear in
the buffer once.

--1030--

Ken, could you increase the limit of 50-words for the user-dictionary?

There's some pretty common words NOT in the dictionary that can quickly
make it hit the 50-word mark.

> toaster, networked, laserjet, i'd, additionally, replacing,
> gathered, gadgets, tags, digitize, megabytes, claiming, email,
> contacted, harassed, rumors, sheets, insured, netmail, fido, newer,
> segments, offline, qwk, replying, compuserve, roots, drafted, docs,
> touring, laptop, delays, incredibly, celebrates, limits,
> announcements, modems, fidonet, announced.

An avg 50-word file is about 400 bytes.
A 100-word file would still use the same 2 blocks of disk space.

Maybe it might even be worth a new SHORT CONFIG value.  I'm sure
someone might want to turn user-dictionaries off entirely (0).  Or
make it fairly large (500).

Also, when the limit is reached please have the speller so.
(I thought all those new [L]earned words were going into my
dictionary, they were just going to NIL: instead.)

Thanks

--1031--

JC> 4. [A]bort logoff and return to BBS.  [D]isconnect now.  Countdown:

JC> Look at example number 4 (logging off upon completion).  It uses letters
JC> enclosed in brackets.  Normally, brackets are used in CNet to show the
JC> default command when you press ENTER.

I still like this method.

I feel it would still be clear to the user, which are choices and which are
defaults (if any).
> Terminal [A]NSI, [C]BM, [I]BM, [S]ky, [NONE]:
> [B]ase, [F]eedback, [G]files, [O]ff, [P]files, [ST]atus, [T]ime
> Voting (Topic#, [L]ist, [N]ew, [Q]uit)>
> [A]bort logoff and return to BBS.  [D]isconnect now.  Countdown:
> Are you sure [Yes]?
> Address to Bill Beogelein [No]?

I posted a vote-topic about this.  I would have much rather attached a VOTE
onto this ITEM, instead.  (But I don't have that power.)
1) No need to force everyone to answer it during log-on.
2) Discussion in item text can follow vote topic.

--1032--

Ken, is there any info about CallHost() values 30,32,34-38?

Also, I need a way to write an executable that will....

...post the text (in a file that I specify) to physical sub #n.
Is this currently possible?  How?  CallHost?  Library calls?

CLI> AutoPost FROM=SysOp SUBJ=Notice TEXT=FileName.txt SUB=173

We'd be seeing A LOT more pfiles (and correctly written ones) if
we had better programming docs.

--1033--

Ken, when I FORWARD some of my mail to another user's full mbox, (as
SysOp) I "should" get the same prompt as if I were sending mail direct:

> That box is full, closed, or unforwardable.  Override [Yes]?

Instead all the mail that I was forwarding was unknowingly going
to NIL:.

--1034--

Here on FW, from the root-udbase, type:
1) FIND AnsiMaster.lzh
2) E1

After you view and exit FIND, you are still at the root-udbase (as you
should be) but the prompt becomes a randomly named sub "0":

> List, Quit, subboard#, ?=Menu
> (0) Just RAM it>
> List, Quit, subboard#, ?=Menu
> (0) Just RAM it> s
> Please select a subboard (L=List)
> List, Quit, subboard#, ?=Menu

BA> After you view and exit FIND, you are still at the root-udbase (as you
BA> should be) but the prompt becomes a randomly named sub "0":
BA> > (0) Just RAM it>
BA> > List, Quit, subboard#, ?=Menu

Ken, there seems to be several other places where CNet seems to get confused
or "lose its way" after a FIND+EXAMINE is executed.

Do you want me to find them all and list them here?
Be better to check that CNET is preserving EVERYTHING before the FIND+EXAMINE
and correctly restoring EVERYTHING afterwards.
The current prompt, all 75-94 MCI codes, which sub, "where" CNet was, current
item #, current response #, etc.

Here's another problem spot from the MAIN menu...
1) NM
2) BROWSE
3) READ 47 (at response 9 of 20)
4) Someone is asking about a certain file, so I FIND+EXAMINE for it.
5) After I hit RETURN back at the "FIND" prompt.  CNet "should" be right back
   at item #47, response #9, in the browse-mode of NM.  It's not.

"FIND+EXAMINE at any prompt" is too handy to not remain "available at ANY
prompt".

Thanks,

--1035--

You can help track this problem down.

I'm finding it 'hanging' on even the simplest of DOS scripts.
(But only at random on about 20% of the calls.)

Try this experiment:

Edit BBSTEXT, changing 1 into 2.
1> @ Logoff complete \n1
2> @ Logoff complete {#4 s:TestScript} \n1

Create a DOS script called "s:TestScript".
Containing something simple like:
> Date TO t:1
> Copy t:1 TO t:2
> Copy t:2 TO t:3

If this simple stuff hangs, I don't think H&S stands a chance.

(Ken, would it be possible to try this on FW for 1 week, also?)

BB> Are multiple ports attempting to run this script concurrently?

In my case, NO.  (I ran with just 1-line all of last week.)

Perhaps a better script to test with, would be the 1 I was actually using.

(Which actually SHOULD work with 1-line or 99-line systems.)

BBSTEXT...
> @ Logoff complete \n1 {#4 CNet:s/RenamePL %23}

CNet:s/RenamePL...
> .key which
> .bra {
> .ket }
> FailAt 21
> Copy sysData:log/Port{which} sysData:log/OldPort{which}

Pretty simple DOS-stuff.

After the "hanging", shell1 is leftover as:
> failat >NIL: 21
> CNet:s/RenamePL 1
> endcli >NIL:

I have a long list of "after-every-caller" house-keeping scripts I'd like to
run.  But I'm waiting until I get this 1 simple "copy-port-log" script working
until I start adding multi-scripts and more-complex-scripts.

--1036--

Suggestion #1036...

When a user re-edits his finger-file (EF), his old answers should
appear in the input-buffer as the defaults.  He could quickly hit
RETURN to preserve those replies and just re-edit the 1 or 2 he wishes
to change.  Currently, if he wishes to change just 1 of 20, he has to
re-answer, in full, all 20 questions.

Needless to say, once callers find that out, it discourages them from
updating their finger-files at all.

--1037--

Paper-mail isn't data-mail.

I use the 2 VERY VERY differently.
Junk mail goes in the garbage.
Everything else gets stockpiled in tons of stacks of paper and metal file
cabnets.

E> You should be using your BBS mailbox just like you do with a REAL
E> mailbox.

But I need old mail available:
1) Online AND
2) Search/scan/sort sub-like powers.

I'd love to forward letters from mail to a sub.
But how?

Store offline, or compressed, or as 1000 tiny files, no #2 powers, or tons of
printed paper, or in a sub as 1000 DLable files, or any other slow/inefficient
means, just doesn't cut it.

One person's needs differ from another.

It does bother anyone that a caller can say they mailed you 1 thing, but you
say differently, but you have absolutely NO record of the now-deleted mail?
They can claim ANYTHING.
I can't possibly remember 1000s of email msgs.

> I'd love to forward letters from mail to a sub.
I could quickly search/scan everything FROM or TO that user.
Over the last x days.
On any subject.
Sorted into any subs.
Keep it all online x days.  (Then it would auto-purge.)
(And all the other things that are already CNet "sub" features.)

>forward letters from mail to a sub.
That's *ALL* I need to do ALL of the above.  (And then some.)

--1038--

Is there an MCI code for "# of new responses to the current item"?
So we can do things like: "This item has 4 new responses."

Also, the "# of overall items in a sub".
So we can do things like: "Item #34 of 98".

--1039--

> Enter this sub's password:

Jim (or anyone) what would be the suggested behavior of these
password-subs during Scans/Browses/FINDs/NSAL/etc?

Prompt EVERY user, EVERY time all those subs are "hit"?
Skip/hide the password prompt, allowing access anyway?

--1040--

Ken, when using RR at the a prompt, would it be possible to make it
check that RECOVERED==TOTAL (and that everything went OK) before it
overwrites the old files with the new ones?

Currently, if something goes wrong, (full ram: or full drive, etc)
RR will abort:
> Recovered:   113/ 186 items;  911/ 1665 responses
but STILL overwrite the old files with the new.

Losing a good portion of all items/responses in that sub.
(Or at least I assume this is what happened to me recently.)

Also, I think #2 would be more flexible than #1.
(Both would have the same default, that is, "the current data path".)
 1> Use RAM: for temp files [No]?
 2> Path to use for temp files: MyDisk:
(We could specify a LARGE partition.)

(Running RR via events, would also need its "RAM" cmd-line option
changed to "specify a path".)

--1041--

WP> CONTROL PANEL's USER INFO; why not add the following:

All good ideas.  I wish these could be squeezed into gadgets right on the
control-panel itself, instead of "select each node, hit UserInfo, select next
node, hit UserInfo...".  Clicking WHERE/DOING/QUEUE right on the control-panel
gadgets would toggle ALL of its node-display "Action Text" into the
WHERE/DOING/QUEUE strings.

(A 3-way cycle-gadget that would toggle through WHERE/DOING/QUEUE.)

I'd also find a separate cycle-gadget "HANDLE/REAL-NAMES" invaluable.
Clicking it would change all the names on the control-panel between HANDLE vs
REAL-NAMES.

I have enough problems remembering names, without remember which
handles are associated with which real-names.  (You effectively have
to remember 2, instead of 1, names for many users.)

And maybe CONFIG/LOGS should allow each log to specify:
>Write HANDLES or REALNAMES or OBEY USER'S PRIVATE-FLAG?

I look at my upload/download logs and wonder "who are all these
strange names" because of the handles.

(I'm the only person that sees those logs, so it would be safe for me to
"ignore the user's 'private-name' flag".)

Maybe also a STATS pull-down menu from the CONTROL-PANEL.

1) Click on any node in the control-panel.
2) Pull-down menu.
3) That node's full STATS-window opens on your workbench screen.

No need to open a node's screen.  (50,000-200,000 bytes of ram.)
Or that node's STATS-window over that screen.

--1042--

> Why won't anyone joinlink (JL) with me...

Perhaps:
1)  99% of the people are using the pre-2.70 non-automated JL'er.
    After the next release comes out, I hope JLing will become commonplace.
    Does anyone know when JL was 1st installed?
    If it's been 1-2 years, and JLing is still so rare, maybe "non-automation"
    could have been the problem.
2)  Does everyone know you are interested?
    I sure wish Ken would set a side a special "meet others interested in
    join-linking" section here.  Or post a news-file telling everyone who's
    interested in JL'ing with others should send mail to a designated acct #n.
    (You perhaps?)  That person would maintain a gfile for names, dates,
    times, phone #s of regular JL sessions.  (It would help to inspire more
    JL'ing, as well as encourage more people to attend those JL'ing sessions.)
    I regularly tell non-CNet SysOps to check-out a few JL sessions to see
    this great feature in action.  They ask when/where.
    I say, "Gee, I don't know".
3)  Cost.
    Maybe JL just once per week instead of twice.  For x/2 hours instead of x.
    Anything is better than nothing.
    Maybe coordinate systems within the same areas.
    It's cheaper to JL together 2-CA, 3-NV, 2-UT systems.
    And 3-FL, 2-GA.
    And then run 1-line between FL and CA to JL all 11-systems together.
    Cheaper than it is to cross-cross all over the country 11-times just
    to do the same thing.

--1043--

At one time was it possible to enter a range of #s at the "Respond, Pass"
prompt and just read those replies?  I'd love that feature.

(We can quickly [S]can that item, but afterwards we can't range-read the
responses we want.)

--1044--

YANK from the "Respond, Pass" prompt seems to yank nothing 75% of the time.  I
can't figure out why.

(On my BBS and FW.)

More info, I tried YANKing this item from the response/pass prompt.
Even though it only has 14 responses, at the 14th response, the system
is now thinking it has more.  And "LAST" doesn't work at all.

> More responses. Respond, Pass, or ENTER to continue> last

> End of item. Respond or Pass> last

> End of item. Respond or Pass> last

Yikes, now it just jumped back and forth between this item (78 responses) and
a different item with only 14 responses.

The one that starts:
J> LUX=352,L
J> Zip_Code="XXXX"
J> Phone="XX XX XX"               (+352) 65 43 21

--1045--

P> I think they are all great ideas.. I have posted many similar ideas
P> as you well know.. But it gets old asking again and again for something.

I'm sure, at 1 time or another, many of these ideas have been kicked around
before.  That's not to say they shouldn't be kicked around again.

Maybe Ken could create (and use freely) some response-macros:
F6 R!;This idea can't be implimented.
F7 R!;This idea will be implimented soon.
F8 R!;This idea won't be implimented.
F9 R!;This idea has been noted and is under consideration.

We'll at least know where we stand.

--1046--

OG>  Another Suggestion, I would like the Xfido to CONVERT MCI into ANSI as it
OG> exports, so a Certain DLG sysop doesn't keep asking me if \cf is line
OG> noise !:) heheheheh!

Is ANSI permitted in many/most Fidonet Echos?

Maybe the "EL" sub definitions for "Disable MCI" could be changed to:
"MCI Handling": Strip/Forbid/ANSI-Convert/Allow

--1047--

Ken, the CONFIG/LOG list would be great if alphabetized.
Also, isn't "NetVote" supposed to be "NewVote"?

--1048--

P> Only one bug to report so far.. I was editing a post in a udbase,
P> repair_sub was working away on another port, when i saved the message

Has any info been given about when/where is it 100% safe to use repair_sub?
Could/should someone with 12 callers online, run RR?

P> Could the AT command be changed to allow us to edit an uploads
P> TRansform status.. Please :)

I can't think of ANY flags/vars that we shouldn't be allowed to edit.
Sooner or later some one is going to want/need to tweek something.
It's always going to be that 1 thing, that "Oh, you can't edit that".
Now that we can AT 100s of items at once, FULL-AT is a must.

P> I like the upload of files.bbs idea!!

It'll be interesting seeing how many people use this, as implimented.
If you pick 9 new files to upload, are you going to edit/create the correct
unique files.bbs each time?

But if each file's description could be INSIDE each archive, each time you
ULed that file, the filenote goes with it.  (But Cnet would have to extract
and handle it.)

Or maybe something like, each file would have a matching description-file.
"foo.lha" has a matching "foo.lha.desc".

Anything is better than creating a custom files.bbs EACH TIME I UPLOAD a
different group of files.

PB> you upload with the file descript of that file titled files.bbs (or you
PB> can change the name in bbstext to whatever you like (thanks again Ken!)

What happens if 20 different Cnets that you call, require 20 different names
instead of using "files.bbs"?  

The only way this will work is if EVERYONE knows the description-file is
ALWAYS called the same thing.

I think we could even convince a large # of shareware authors...
"Make sure all your archives have a file named <whatever> in them.  That
becomes the filenote online, too."

The filenotes would be the exact text/dates/version#s/authors that the
original author wants.  (Even though the UL has passed though 47 different
BBSes.)

P> Now how about a downloading
P> of a files.bbs description file

This would be slick, if done something like:
BBSTEXT> c:Filenote "{V87}" "{V95}"
That line would be used to create the user's files.bbs.
After the DLing, just type "Execute files.bbs" and all your DL are filenoted.
(Of course, your BBSTEXT-line could be as long, short, or different as you
like.)

--1049--

Ken, do you think I could get you to add 1 new MCI-code that would
expand into a file's full path+name (UDBASE3:MyDir/Foo.lha).

In arexx, it can be tricky trying to piece those 4 bits of info together
myself.  (BASE or UDBASE, partition #, dir, filename.)
(And the XT#####'s can change anyway...)

A new "permanent" 'getuser' value would open up a whole world of
possibilities for things that need to be done to the files WITHIN an
archive.

Viewing, extracting, virus-checking, reading 1 txt-file, extracting+DL
1 file, extracting several files from several different archives and
repack for DLing, viewing IFF/ANSI pictures online, checking version #s,
etc, etc, and etc.

-thanks

--EOF--

Sunday 04-Jul-93 20:39:17

-Bill "Mr. BBS" Beogelein, 810-473-2020, 2-line HST 14.4k USR DS, 1:2410/207

