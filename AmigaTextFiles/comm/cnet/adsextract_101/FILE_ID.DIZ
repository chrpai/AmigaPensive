AdsExtract is a program that will scan pre-configured directories for files matching a user-specified pattern and when found, will extract the "short" description from those files and place them, plus the matching filename to which they belong, in a user-specified "files.bbs" file. [METALSOFT]
