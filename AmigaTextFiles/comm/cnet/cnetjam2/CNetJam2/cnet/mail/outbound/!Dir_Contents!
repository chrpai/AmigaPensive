1/28/95

Directory of "Mail:outbound/":  (also assigned as OUTBOUND:)

    - packets/

        - This directory is used by JamScan as a temporary storage directory
          when packing files. Do NOT store ANY of your OWN files in this
          directory.

    - The OUTBOUND directory is where files LEAVING your bbs are stored
      until the next time the poll script is run. HOLD and CRASH mail, as
      well as File Attaches are also stored in this directory.

Dotoran
