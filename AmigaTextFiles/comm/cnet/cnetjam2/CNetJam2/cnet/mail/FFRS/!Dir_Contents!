1/28/95

Directory of "Mail:FFRS/":     (Also assigned as FREQ:)

    - bin/
    - configs/
    - docs/
    - groupie/
    - rexx/
    - scripts/

        - These directories came with the FFRS package. No changes were made
          to these directories.

    - !Error
    - !Footer
    - !Footer2
    - !Frontiers
    - !Header
    - !Header2

        - These are text files that are either included in user's freq
          reports, or made part of one of my filelists.

        - See the "ffrs.cfg" file for uses of all files above NOT ending
          in the number 2.

        - See the "FREQ-List_Maker2" file, inside the "pfiles:fido/"
          directory for info on ones ending with 2.

    - ffrs.cfg

        - The config file which specifies how I wish to run FFRS.

    - FFRS.index

        - The index file, created by the FFRI file found in the "Bin/"
          directory of this directory. It uses information found in the
          "ffrs.cfg" file above to create a binary tree index for QUICK
          location of files during FREQ's.

    - FR_AFILES
    - FR_AFILES.LHA
    - FR_CFILES
    - FR_CFILES.LHA
    - FR_DFILES
    - FR_DFILES.LHA
    - FR_FILES
    - FR_FILES.LHA
    - FR_IFILES
    - FR_IFILES.ZIP

        - These are the files created by the "FREQ-List_Maker2" file, found
          in the "pfiles:fido/" directory.

Dotoran