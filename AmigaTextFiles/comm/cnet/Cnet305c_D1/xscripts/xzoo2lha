.KEY NEWN,PATHF,FN
.BRA {
.KET }

cd ram:
makedir {FN}
cd ram:{FN}
changetaskpri -1
zoo xSO// "{PATHF}"
lha -m -x -a -r -X -2 a "{NEWN}" #? s:MyBBS.Displayme
;	     Edit the above path filename   ^^^^^Edit this^^
cd ram:
delete ram:{FN} ALL
