                          Network Secretary v1.3
                           By: Carlos A. Roldan


     CNet's built in network aliases is great but many users don't often use it
correctly.  Also, they sometimes forget the correct syntax  to  use  when  they
send FIDO  or  UUCP netmail.  This  program  is  replacement  for  the  network
aliasing offered by C-Net.

                                 Features

 - Each user can have an unlimited number of aliases.

 - Optional comment for each aliases.  (Who address is for?)

 - When sending netmail from the program, the user  is  presented  their  alias
   list and can select them by number.

 - Allows adding, killing, listing, and editing of items in the alias list.

 - Simple to use and install.


                               Installation

1. Copy to Net.rx to PFILES:Netmail.

2. Copy "Menu" to the same path.  This  file  should  always  be  in  the  same
   directory that the main program resides.

3. Add to the "Available Anywhere" section of BBSMENU.  Use discretion  as  far
   as the command to be entered.  I use NETmail.

	Example:  NETmail |#0PFiles:Netmail/Net.rx


4. Copy Qsort to your C directory.

5. Load your text and menus and you should be all set!


*Note : This program will create a file "_NETSEC" in the users UUCP directory.
*Note2: You may edit the "Menu" file as you wish.  You may use Ansii, Ascii  or
        MCI codes in this file.
*Note3: Qsort is (c) 1991 Michael D. G.

                               Registration

     Network Secretary is donation ware.  The more donations I get, the  more I
will update the program.  Simply send $2 to:

                             Carlos A. Roldan
                              6975  Parkwood
                            Detroit,  MI 48210
                                   USA


Other programs available:

COMPULIST???.LHA                  Easy to use BBS list program.

COMPUMENU???.LHA                  Easy to use menu program, use it for
                                  displaying anything you want (Games,
                                  Text files, ect...)

BDAY???.LHA                       Sends mail to users on their birthday.


HISTORY
=======

v1.0  First release, no known bugs.
v1.1  Made some routines slightly smaller.
v1.2  Some slight visual changes.  Uploaded to Aminet.
v1.3  Cleaned up the code a bit.
